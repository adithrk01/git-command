command for listing confygration listing
    >git config --list

command for adding user name
    >git config --global user.name"username"

command for adding email
    >git config --global user.email"user.@gmail"

command for initializatio git repositry
    >git init

command for listing all untracked files
    >git status

commandfor addingall untracked files to staging area
    >git add -A
    
command for adding all staged files to local repositry
    >git commit -m"message"

command for adding remote reporstery
    >git remote add name remote_path

command for addikng changes in local reporsteryto remote reporstery
    >git pish -u remote_name branch_name
    